# Employee-Database-Management-System
A full-stack web application designed to manage employee records efficiently. The system supports CRUD operations (Create, Read, Update, Delete) for employee details with secure backend integration and a responsive frontend interface. 
